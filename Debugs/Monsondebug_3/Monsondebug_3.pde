float angle;
void setup() {
  size(500,500);
}

void draw() {
  ellipse(width/2, height/2, randomWidth(), height * sin(angle));  
  angle++

void randomWidth() {
 float val = random(width); 
}

void mousePressed() {
  background(127;  
}
